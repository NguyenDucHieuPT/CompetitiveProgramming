    //Cho một số nguyên không âm N, hãy in ra chữ số đầu tiên của N.
    int NumberOfDigits(ll n)
	{
		if (n < 10)
			return n;
		return NumberOfDigits(n / 10);
	}
	void process(void)
	{
		ll n;
		cin >> n;
		cout << NumberOfDigits(n);
	}